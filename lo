#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"

if ! command -v go >/dev/null 2>&1; then
  echo "❌ Go is required to run this development wrapper." >&2
  exit 1
fi

exec go run "$SCRIPT_DIR/cmd/lo" "$@"
